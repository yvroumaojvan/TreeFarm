# 🎯 任务策划书：TreeFarm 安全盲区终结战（v2 · 并入 91 轮答卷情报）

> 启动：2026-09-24 · 驱动：《TreeFarm-30轮检测报告》+《TreeFarm-91轮检测最终答卷》（别的 AI 跑出，盲区已代码实锤）
> 铁律：**泛化修复，绝不对题打补丁**（乖宝钦点："别人拿题的变体测，照样能测出 bug"）
> 每条新规则必须配 **3-5 个变体测试**（换函数名/嵌套/跨行/混淆），变体过才算真会

## 一、要终结的盲区全景（30轮 + 91轮 合并）

| 编号 | 盲区 | 实锤证据 | 修复策略（泛化） |
|---|---|---|---|
| B1 | **PHP/Go/Rust/Kotlin 安全扫描缺失（4 语言）** | `_SECURITY_EXTS`（analysis.py L1467）实测无 .php/.go/.rs/.kt；91轮 R35/R36/R65 实锤 | 扩 `_SECURITY_EXTS` + 新建 4 个语言安全扫描器，**复用现有污点数据流思路**（输入→拼接→危险调用），跟 Python/Java 同构 |
| B2 | **混淆对抗 4 绕过**：chr 编码拼接 / eval 拼接 / 弱哈希单独 / **反射调用** | 30轮 R17/R20/R22/R24 + 91轮 R41-55 锁定 | ①编码函数族（chr/unichr/String.fromCharCode/bytes.fromhex/hexlify/codecs）→危险调用数据流 ②eval/exec/compile/Function/new Function 动态串 ③MD5/SHA1 出现在密码/认证/令牌场景 ④**反射**（getattr/globals()[]/__dict__/obj[attr] 动态取方法再调用） |
| B3 | **二次注入**（转义后拼接再入危险函数） | 91轮 R75 | 污点引擎加"转义函数输出仍标污点"传播规则（escape/quote/sanitize 的产物在特定 sink 仍危险），需防误报（只有再接危险操作才报） |
| B4 | **2026 新形态 CVE 组**：tar 遍历 / 维护路径穿越 / 弱密钥（BitLocker/加密薄弱） | 91轮 R81-91 检出 3/10 | 通用模式：归档解压路径校验缺失（tar/zip/rar → 写入路径含 ../）、密钥长度/算法薄弱（AES 弱模式/固定 IV/RSA 短密钥）、维护接口未鉴权 |
| B5 | **语义型盲区**（CSRF/UAF/越界/整数溢出/认证授权） | 91轮 R4/R6/R8/R12 等 11 类 | **本次不硬冲**（设计与 README 一致：语义型归思维树兜底），记录在案；README 补认边界说明 |

## 二、执行阶段（每阶段 = 测试红 → 修复 → 回归绿）

- **P0 测试护城河**（先写战场再打仗）：新建 `test_security_langs.py`（B1 四语言）+ `test_security_obfuscation3.py`（B2/B3）+ `test_security_2026cve.py`（B4），每条规则 3-5 变体，全部先红（证明抓到漏洞）
- **P1 PHP 扫描器** `_scan_php_security`：mysqli/PDO SQL 拼接、shell_exec/system/exec/passthru/proc_open、$_GET/$_POST/$_REQUEST 污点源、file_get_contents 路径、unserialize、md5 密码比对、include 动态
- **P2 Go 扫描器** `_scan_go_security`：os/exec（exec.Command/CommandContext/os.StartProcess/syscall.Exec）、net/http 服务端污点、数据库驱动 SQL 拼接、text/template 模板注入、硬编码密钥
- **P3 Rust 扫描器** `_scan_rust_security`：Command::new/std::process、format! 拼 SQL（rusqlite/sqlx/sea-orm）、unsafe 告警（提示级）、硬编码
- **P4 Kotlin 扫描器** `_scan_kt_security`：Runtime.getRuntime().exec/ProcessBuilder、rawQuery/execSQL 拼接、JNI 动态加载
- **P5 混淆对抗泛化**（B2/B3 注入各语言扫描器 + 正则补盲）：编码函数族 / eval 三兄弟 / .html() / 弱哈希语义 / 反射调用 / 二次注入传播
- **P6 2026 CVE 规则**（B4）：tar/zip 遍历、弱密钥、维护接口
- **P7 收口**：`_SECURITY_EXTS` 扩集（+php/+go/+rs/+kt 且保留 .kt 性能链兼容）、全量回归（983 + 新增）、vuln_lab 靶场复扫对比
- **P8 双回归 + 发版**：30轮+91轮 报告原题全过 + 自造变体全过（泛化证明）、CHANGELOG/VERSION 更新、push GitHub 触发云端 CI

## 三、验证标准（乖宝验收）

1. 30轮 R10/R17/R20/R22/R24 + 91轮 R35/R36/R65/R75/R81-91 原题 → 全检出 ✅
2. 每题 3-5 个变体 → 全检出 ✅（泛化铁证）
3. 全量 unittest 全绿 + 云端 CI 绿
4. 无回归：原 983 测试零破坏
5. 综合检出率 57% → 75%+（新增 4 语言 + 混淆/二注/2026 组后估算）

## 四、工期策略

- 每阶段小步提交（阶段性 commit 可回溯）
- 慢节奏防限流；子任务频繁联网才考虑子代理，否则主线程干
- 30轮报告时已确认：Go/Rust/Kotlin **解析已存在**（语言链路复用），只补安全扫描层

## 状态追踪
- [x] P0 测试护城河（21 用例，先红后绿）
- [x] P1 PHP 扫描器（SQLi 三形态/命令注入/unserialize/md5/include 动态）
- [x] P2 Go 扫描器（exec.Command/SQLi/SSRF/硬编码密钥）
- [x] P3 Rust 扫描器（Command::new/format! SQLi/unsafe 提示）
- [x] P4 Kotlin 扫描器（Runtime.exec/ProcessBuilder/rawQuery/execSQL）
- [x] P7 前置：_SECURITY_EXTS 扩集（+php/+go/+rs/+kt）
- [ ] P5 混淆对抗泛化（chr 拼接/eval/.html()/弱哈希语义/反射/二次注入）
- [ ] P6 2026 CVE 规则（tar/zip 遍历、弱密钥、维护接口）
- [x] P0-P4 全量回归 1004/1004 全绿（983 原测试零破坏）
- [ ] P8 双回归（30/91 轮原题 + 变体）+ 发版推送