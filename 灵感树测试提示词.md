灵感树（创新分支）· 强命令版测试提示词
========================================
（2026-09-22 创新分支 v1.0，用于让 AI 对灵感树引擎做权威复测）

## 给 AI 的铁律（先读）
1. 逐条执行下面的命令，一次只跑一条，不要合并、不要跳过、不要自作聪明
2. 每条命令输出四件套：①命令原文 ②完整输出 ③预期 vs 实际 ④结论（PASS/FAIL）
3. 全程只测功能，不要编造输出，输出什么样就报什么样
4. 禁止询问"要不要继续"，直接一条条跑完
5. 这是纯功能测试，不涉及任何个人信息

## 环境准备
- 引擎目录：tree-of-thought/scripts/
- 三条命令之一：node tree.js bug / node tree.js idea / node innovation.js / node harvest_view.js

## 测试组 A：灵感树命令流（核心 7 命令）
A1. 状态初始化检查
    node innovation.js status
    （预期：显示"内核: 空 · 枝丫 0 · 果实库 N 颗"，不报错）

A2. 种内核
    node innovation.js idea "如何让机器学会提出问题"
    （预期：显示"内核已种下"，提示下一步 forms）

A3. 长表达形式枝丫（3 个分支）
    node innovation.js forms '[{"form":"算法","idea":"搜索问题空间找未知","why":"问题=搜索目标"},{"form":"比喻","idea":"机器像好奇的孩子问为什么","why":"提问=好奇心的外化"},{"form":"机制","idea":"检测知识缺口自动生成问题","why":"弱信号=被忽略的缺口"}]'
    （预期：长出 3 个枝丫 n_2 n_3 n_4，各含 form/idea）

A4. 双维评分（novelty 权重 40% 最高）
    node innovation.js score n_2 '{"relevance":0.9,"novelty":0.6,"expressiveness":0.7,"feasibility":0.9}'
    node innovation.js score n_3 '{"relevance":0.6,"novelty":0.95,"expressiveness":0.9,"feasibility":0.5}'
    node innovation.js score n_4 '{"relevance":0.85,"novelty":0.8,"expressiveness":0.8,"feasibility":0.8}'
    （预期：每节点出综合分；n_3 新颖最高但适配低，综合应居中——验证 novelty 权重真实生效）

A5. 摘果收敛
    node innovation.js converge
    （预期：选出综合分最高的表达形式，输出内核/表达/为什么适配/综合分）

A6. 入库（果实库增加）
    node innovation.js harvest
    （预期：显示果实已入库 + 当前果实总数比上次 +1）

A7. 联想（相似果实）
    node innovation.js recall "机器怎么提出新问题"
    （预期：按相似度列出旧果实，提示"把旧表达套在新内核上"）

## 测试组 B：双模式入口
B1. 抓虫树模式转发
    node tree.js bug init "测试抓虫树" --beam 2
    （预期：显示"🐛 抓虫树模式" + 思维树初始化成功）

B2. 灵感树模式转发
    node tree.js idea status
    （预期：显示"💡 灵感树模式" + 灵感树状态）

B3. 未知模式拒绝
    node tree.js unknown xx
    （预期：报"未知模式"，退出码非 0）

## 测试组 C：果实库可视化
C1. 生成可视化页面
    node harvest_view.js
    （预期：生成 harvest_view.html，显示果实 N 颗）

C2. 相似联想矩阵（≥2 颗果实才有输出）
    重复 harvest 至少 2 次后：
    node harvest_view.js
    （预期：HTML 内含"相似"数据或"0 对"——都不算失败，如实报告）

## 测试组 D：健壮性（边界）
D1. 空状态 status
    node innovation.js --state /tmp/empty_test.json status
    （预期：显示"内核: 空"，不崩）

D2. 坏 JSON 分支
    node innovation.js idea "测试" && node innovation.js forms 'not-json'
    （预期：报"JSON 解析失败"，不崩）

D3. 评分不存在节点
    node innovation.js score n_999 '{"relevance":1}'
    （预期：报"节点不存在"，不崩）

D4. converge 无评分
    node innovation.js --state /tmp/empty_test.json converge
    （预期：报"还没有评分节点"，不崩）

## 判定标准
- 组 A 7 条全 PASS = 灵感树核心功能合格
- 组 B 3 条全 PASS = 双模式打通合格
- 组 C 不崩 + 生成文件 = 可视化合格
- 组 D 4 条全 PASS = 健壮性合格
- 任一 FAIL：如实记录输出，交回修复，禁止自行"理解后放行"

## 报告格式（收尾）
最后输出一张表：组别 | 用例 | PASS/FAIL | 备注
